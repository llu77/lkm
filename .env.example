# ========================================
# LKM HR System - Environment Variables Template
# نموذج متغيرات البيئة لنظام إدارة الموارد البشرية
# ========================================

# Development Mode (وضع التطوير)
# Set to 'true' to bypass authentication during development
# اضبطه على 'true' لتجاوز المصادقة أثناء التطوير
VITE_DEV_MODE=true

# ========================================
# Convex Backend Configuration
# ========================================

# Convex Deployment URL (رابط نشر Convex)
# Get this from: https://dashboard.convex.dev/
# احصل عليه من: https://dashboard.convex.dev/
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Deployment ID (معرّف النشر)
# This is automatically generated by Convex
# يُنشأ تلقائياً بواسطة Convex
CONVEX_DEPLOYMENT=your-deployment-id-here

# ========================================
# Hercules OIDC Authentication (اختياري)
# ========================================

# OIDC Authority URL
VITE_HERCULES_OIDC_AUTHORITY=https://accounts.hercules.app

# OIDC Client ID (get from Hercules dashboard)
# معرّف العميل (احصل عليه من لوحة Hercules)
VITE_HERCULES_OIDC_CLIENT_ID=your-client-id-here

# OIDC Configuration
VITE_HERCULES_OIDC_PROMPT=select_account
VITE_HERCULES_OIDC_RESPONSE_TYPE=code
VITE_HERCULES_OIDC_SCOPE=openid profile email

# OIDC Redirect URI (defaults to {origin}/auth/callback)
# VITE_HERCULES_OIDC_REDIRECT_URI=

# ========================================
# Frontend Passwords (كلمات المرور)
# ========================================

# Payroll Page Password (كلمة مرور صفحة الرواتب)
# IMPORTANT: Change this in production!
# مهم: غيّر هذه في الإنتاج!
VITE_PAYROLL_PASSWORD=your-strong-password-here

# Employees Page Password (كلمة مرور صفحة الموظفين)
# IMPORTANT: Change this in production!
# مهم: غيّر هذه في الإنتاج!
VITE_EMPLOYEES_PASSWORD=your-strong-password-here

# ========================================
# IMPORTANT NOTES (ملاحظات مهمة)
# ========================================

# 1. MANAGE_REQUESTS_PASSWORD is NOT here!
#    It's stored in Convex Backend for security.
#    كلمة مرور إدارة الطلبات ليست هنا!
#    إنها محفوظة في Convex Backend للأمان.
#
#    To set it, run:
#    لضبطها، شغّل:
#    npx convex env set MANAGE_REQUESTS_PASSWORD "your-password"

# 2. Never commit .env.local to Git!
#    لا ترفع ملف .env.local على Git أبداً!
#    Add it to .gitignore
#    أضفه إلى .gitignore

# 3. For Cloudflare Pages deployment:
#    للنشر على Cloudflare Pages:
#    - Add these variables in Cloudflare Dashboard
#    - أضف هذه المتغيرات في لوحة Cloudflare
#    - Choose "Production and Preview" environment type
#    - اختر نوع البيئة "Production and Preview"

# 4. See ALL_SECRETS_AND_KEYS.md for complete guide
#    راجع ALL_SECRETS_AND_KEYS.md للدليل الكامل

# ========================================
# Optional: Hercules Website ID (اختياري)
# ========================================

# VITE_HERCULES_WEBSITE_ID=your-website-id

# ========================================
# Created by: Claude Code
# Date: 2025-10-25
# ========================================
